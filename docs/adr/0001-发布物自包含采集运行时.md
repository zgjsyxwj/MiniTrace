# 发布物自包含采集运行时

MiniTrace 的正式发布物必须包含运行采集后端所需的运行时和 Frida 组件，不能要求用户另行安装 Python、Frida 或命令行工具。这样会增加安装包体积及各平台构建维护成本，但能保证内部分析成员获得可重复的运行环境，并避免本机依赖版本漂移导致采集结果不同。

当前阶段只在开发者本机从源码运行；完成 macOS 明文边界验证后，正式发布物再执行上述自包含要求。
