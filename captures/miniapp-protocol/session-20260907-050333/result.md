# 道具使用补采

本轮使用 flue，目标 PID 19804。采集器于 2026-09-07 05:03:33 启动，确认双向 `16001/16021` 心跳后由用户操作，收到“完成”后停止。会话共保存 41 个游戏帧：12 个请求、29 个响应；没有未拆分游戏载荷。

停止时 Ctrl+C 结束了 PTY 宿主，采集器的 `finally` 未执行，因此原始 `summary.json` 缺失。现有 `timeline.jsonl`、`transport-events.jsonl` 与 41 个原始帧逐项一致，传输事件中没有 `frida-error`；据此补建的 [summary.json](summary.json) 显式记录 `recoveredAfterForcedPtyInterrupt=true`。最后一帧时间为 05:04:30。该恢复只说明已落盘证据完整，不把缺失的原退出路径冒充实测。

## 使用人物经验卷

槽位 44 来自上一连续会话中新手大礼包开出的 `40007`“人物经验卷(30000）”。本轮请求为 `bagMode=11、operation=3、slotPos=44、quantity=1、confirm=false、itemType=28`。请求自身不携带物品 ID，身份由前序槽位证据确认。

成功响应模式为 `BAG_ADD_EXP=11`：增加人物经验 30000，提升 5 级，升级后经验 `6686/9300`，属性点 `SP=450`，能力点增加 `CP=15`；第二经验和第二等级增量均为 0。随后战力同步为 4563。

- 请求：[000012-request-12001.bin](frames/000012-request-12001.bin)，SHA-256 `fbc0f1178b8aad5cd42ae15b32ff735860765f1ed31a87ac61b5e45e4feee462`。
- 响应：[000027-response-12001.bin](frames/000027-response-12001.bin)，SHA-256 `c5ed7b1199e6f256bc31fd919116b016f0a1d8789ad2d298c4b178a582901200`。

## 使用20级秘宝盒

槽位 31 是上一连续会话开新手大礼包后由 `1324`“20级秘宝盒”复用的槽位。本轮请求与此前使用新手大礼包的通用请求逐字节相同，因此物品身份仍必须结合槽位时序，不可由请求体单独推断。

成功响应模式为 `BAG_USE_CHEST=4`，额外消耗数量为 0，只返回 1 个新物品：`413`“加1敏捷宝石(绑）”×1，复用槽位 31；物品类型 30、图标 64、可叠加 99、自动绑定，属性为敏捷 `5 +1`。这只证实本次开盒结果，不推断固定掉落或概率。战力同步仍为 4563。

- 请求：[000034-request-12001.bin](frames/000034-request-12001.bin)，SHA-256 `cbdcfe02f4fd024ea4fd9354d2f58ef5ac6083b77ebac52d225619d9f585ab30`。
- 响应：[000035-response-12001.bin](frames/000035-response-12001.bin)，SHA-256 `c6177d1dcd2bad1738e433c4f0e924d5998a48636913cb740e434c42b448725c`。

## 辅助帧与边界

升级后收到两个 `11014` 系统邮件通知，并自动发送两次空的 `11002` 邮件统计请求；两个统计响应相同。还收到 `14563`，字段为一个成就分类 `2` 的可领奖提示。源码分别确认其为邮件通知、八类邮件计数和成就红点提示，但本轮没有打开邮件、领取附件或领取成就奖励；其离线玩法取舍尚未确认，暂不加入有用或忽略协议列表。

既有忽略项共 28 帧：`16001/16021` 心跳与校时、`13510` 世界聊天、`20401` 战令和 `27403` 等级开放提示。没有新增忽略规则。

运行 `python captures/miniapp-protocol/session-20260907-050333/analyze.py` 可复核全部 41 个帧的长度、消息号和 SHA-256，解码 6 个核心帧、7 个辅助帧并跳过 28 个既有忽略帧；两个 `12001` 成功响应均执行截断拒绝检查。静态依据为 `main.min.js:20718` 请求编码、`:22158` 使用操作、`:22169`/`:22176` 响应模式、`:127574` 经验分支、`:127665` 开箱分支和 `:21291` 升级数据读取。

本轮未修改 Android 源码、资源或存档。完整解析只适用于这两次成功使用；失败、背包已满、一次使用多个、其他经验卷和其他秘宝盒仍未采集。
