# 当前会话交接

日期：2026-09-07

## 用户目标

制作一个用于微信小游戏抓包的桌面端应用。用户操作小游戏时，应用持续自动记录请求；随后可查看、筛选、分析并归档本轮记录。

## 本轮用户原话

> 我想做一个抓包小游戏的桌面端应用，我操作时就会自动记录所有请求。当前有哪些已有的文档能够作为参考文档。

> 将相关文件打包压缩放到桌面，将当前会话也放里面。方便我到其他会话开工

## 已确认结论

- 项目已有成熟采集核心 `capture_miniapp_protocol.py`，桌面端应优先包装它，不重写 Frida、拆帧和归档逻辑。
- 默认采集微信 `WeChatAppEx.exe` 的 `flue.dll` 明文边界；WinHTTP 不是默认路径。
- 推荐桌面状态机：选择网络进程 → 启动采集 → 等待 ready 和双向心跳 → 用户连续操作 → 停止采集 → 校验并展示结果。
- 采集器记录所有可识别游戏帧；不能拆分的明文载荷保存到 `unparsed-payloads/`。这不等于全系统通用 HTTP 抓包。
- 每次会话必须保留原始 `.bin`、`timeline.jsonl`、`transport-events.jsonl`、`ready.json`、`summary.json` 和 SHA-256。
- `wechat-miniapp/api-cg-index.csv` 可用于显示消息号名称；有用与忽略协议列表可作为默认分类和过滤依据。
- 停止时必须让采集器正常收到中断并确认 `summary.json` 落盘，不能只结束终端宿主。

## 建议新会话的第一项工作

先检查项目已有桌面技术栈和依赖，然后为 `capture_miniapp_protocol.py` 做最小桌面壳层。首版只需要：进程选择、开始、状态、停止、实时帧列表、打开会话目录和错误显示。

## 当前未完成

- 尚未确定桌面 UI 技术栈。
- 尚未创建桌面应用源码。
- 尚未定义首版详细界面。
- 尚未验证现有采集器在当前微信版本上的 RVA。

## 包内容说明

压缩包包含核心文档、采集脚本、协议索引和若干代表性采集会话。项目中的全部历史会话没有全部复制；它们仍保存在 `captures/miniapp-protocol/`。
