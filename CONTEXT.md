# 项目方向

`android-game` 是《世界 Online》Android 客户端恢复工程。目标是得到可维护、可编译、可运行并逐步离线化的恢复版客户端，而不是重新打包原始 DEX，也不是 `h5-game` 的 H5 客户端分支。

- `app` 是当前唯一 Android 应用模块；`app/src/main/java/` 是持续修复的 Java 源码，`app/src/main/res/` 是恢复后的 Android 资源。
- 恢复版保留原游戏的界面、交互和玩法结构。依赖原服务器或保护库的路径按任务逐项替换为本地行为。
- 启动完成本地初始化后直接进入人物选择页，不显示登录页，不建立外部网络连接。
- 恢复版不提供普通摆摊或附魔摆摊。背包、NPC 商店和自身装备附魔是独立功能，不随摆摊一起删除。
- 离线角色已有 `missions.inventory` 时，以其中的物品槽位快照恢复背包；任务奖励清单是发奖记录，不再次用于重建物品。任务奖励与背包快照使用同一次角色存档事务保存。
- 任务选领奖励和礼包消费复用同一背包事务。小游戏道具图标来自 UI 图集元数据 `assets/data/sheet.json`；角色 `sprite/fragment` 与物品图标编号空间不同。
- 任务切图目的地保存在 `missions.location`，与交付及奖励同次写入。当前离线世界之门使用入口编号 900，整体采用小游戏迎客殿 4 的布局、NPC 和坐标；神剑广场采用小游戏编号 16。位置中的 `sourceMapId` 标识坐标所属资源，旧 900 坐标不能直接用于新布局。
- 用户称 Android 目标为“世界之门”、小游戏目标为“迎客殿”。上述 4→900 是明确的离线适配；采集中的原始 4 与原始 900 仍是不同布局和任务链，不能据此合并任务编号。历史 Android 编号 30680、神剑广场导出编号 31414 只作证据。核对与实现边界见 `docs/小程序协议到Android离线适配.md` 的“迎客殿整体适配与神剑广场”。
- 离线战力是常态属性的本地综合评分，不还原服务端公式、不参与伤害结算、不单独存档。统一由 `OfflineCombatPower` 在读档、任务刷新、确认加点和换装后计算。
- 已接入战斗组的普通敌人可以重复挑战。胜利保存后复用原 NPC 的 150 次地图更新消失/刷新流程；任务击杀只累计匹配的已接目标并封顶，战斗经验与铜币按每场胜利结算。剧情 NPC 继续按任务条件显隐。
- 战斗组中的 NPC 是临时助战：仅在对应已接任务仍有该战斗组可推进的击杀目标时加入。多目标任务须检查战斗组的全部击杀项，不能只比较第一项。每次开战按任务状态重算名单，不作为常驻队友写入存档。
- 本地角色存档以 SQLite 为权威：`role_saves` 保存每个槽位的完整角色 JSON，`metadata` 保存数据库版本和迁移状态等元数据。`files/offline/roles.json` 仅作为首次迁移源及原子交换/恢复副本，正常读取不依赖它。
- 静态任务、NPC、地图、物品和商店定义仍从 APK 内 `offline` assets 读取，不迁移到 SQLite。任务进度、奖励记录、背包槽位快照和地图位置的同一事务语义保持不变；写入失败时不得发布内存状态。
- 当前进度、已知阻塞和验证结果以 `build/` 下的本次构建证据及 `docs/` 中对应专项报告为准；`CONTEXT.md` 不承担工作日志职责。

## 证据与代码边界

语义证据按以下优先级使用：

1. 可重复观察的原始客户端运行行为。
2. `analysis-tools/runtime-classes.dex` 及其只读 Smali。
3. 原始 APK、`apk-extracted/` 中的资源和清单。
4. JADX、CFR 等反编译结果；它们是恢复候选，不是语义真相。

目录含义：

- `app/src/main/java/com/hz/`：主要游戏逻辑。
- `app/src/main/java/com/lori/`：Android 宿主、J2ME 适配和公共能力。
- `app/src/main/compat/`：仅用于恢复工程编译或替换边界的兼容代码。
- `decompiled/`、`build/*recovery*/`：只读对照材料和恢复证据，不作为直接打包源码。
- `apk-extracted/assets/`：恢复版当前复用的原始游戏资源。
- `resources-restored/`、`maps-local/`、`map-restored/`、`animation-*/`：已恢复或验证的本地资源及样例；完整性以各自报告为准。
- `docs/`：专项分析和历史记录。其中部分文档来自 H5 分支，只作证据参考，不决定 Android 恢复版方向。

## 稳定调用边界

启动主链为：`PipActivity` → `GameMIDlet` → J2ME `Display`/游戏线程。资源读取、网络收发和原保护调用是独立边界；修改其中一条路径时，不默认其他路径已经恢复。

`com.hz` 中大量短名称来自混淆和反编译。只有在 DEX/Smali、调用方或运行行为足以证明职责时，才恢复类、方法、字段和变量的语义名称。名称恢复后统一使用新名称，不同时维护旧名称别名。

小游戏 JS 可提供同源功能的语义名称和对照实现。使用前逐项核对 Android 的字段顺序、数值宽度、分支和调用方；跨版本差异保留 Android 语义基线，不能因 JS 名称相似就整体移植。成就数据入口已恢复为 `com.hz.d.Achievement`，原始证据仍使用 `com/hz/d/a.smali`。

## 统一语言

**原始客户端**：用户提供的原始 APK 及其实际运行实例。它用于行为对照和证据采集。

**恢复版**：由 `app` 模块源码重新编译的 Android 应用。它与修改原始 APK 或重新打包 DEX 是不同产物。

**恢复源码**：位于 `app/src/main/`、经过证据核对并可继续维护的源码和资源。反编译输出本身不称为恢复源码。

**语义基线**：`analysis-tools/runtime-classes.dex` 及对应 Smali。用于核对方法签名、数值类型、字段访问和控制流。

**还原行为**：有原始客户端、DEX/Smali 或资源证据支持，并按原语义恢复的行为。

**离线替代**：为消除服务器、保护库或第三方在线能力依赖而实现的本地行为。它应复用原后续流程，但不冒充已还原的服务端行为。

**登录成功后续流程**：原登录成功后已经存在的页面或状态入口。离线启动自动进入该入口，不复制一套新的角色选择或进图流程。

**保护边界**：原客户端通过 NetHTProtect、`NHPProtect` 或相关 JNI 执行资源及通信处理的位置。恢复版在这些位置接入本地实现，不加载或打包网易保护运行库。

**本地资源**：保存在本项目目录内、运行时无需远程服务即可读取的资源。只有文件存在且解析或显示验证通过时，才称为可用本地资源。

**可编译**：Gradle 编译或 APK 组装成功。它不代表应用已启动、界面正常或离线可玩。

**离线可玩**：在外部网络不可用时，恢复版仍能从启动入口进入游戏，并完成当前任务明确要求的玩法流程。

## 桌面采集工具统一语言

**MiniTrace**：位于独立仓库 `zgjsyxwj/MiniTrace` 的《世界 Online》微信小游戏本地分析产品，首要服务恢复工程，也允许熟悉协议分析的内部成员单独使用。
_避免使用_：通用抓包工具、Android 恢复工具

**被动采集**：只观察并保存目标进程已经产生的流量，不重放、修改或主动发送业务请求。进程附加本身不称为主动请求。
_避免使用_：只读抓包

**《世界 Online》小游戏明文流量**：《世界 Online》小游戏进程产生的 HTTP、HTTPS 和 WebSocket 明文数据。可识别的游戏协议帧和未分类明文载荷都属于此范围；微信其他功能、其他小游戏及操作系统其他应用的流量不属于此范围。
_避免使用_：所有请求、微信全部流量、系统流量

**游戏协议帧**：从小游戏 WebSocket 明文载荷中按游戏帧格式完整拆出的请求或响应。
_避免使用_：HTTP 请求

**未分类明文载荷**：属于小游戏明文流量，但尚不能按已知游戏帧格式完整拆分的数据。未分类不表示无价值或可以丢弃。
_避免使用_：无用流量、解析失败垃圾

**采集会话**：一次用户操作周期从开始采集到正常停止或失败结束的完整本地证据集合。一条采集会话只对应一个小游戏运行链，可以跨越承载进程重启；会话只保存在本机，默认不上传或同步。
_避免使用_：抓包文件、日志

**不完整会话**：因挂载失败或异常退出而无法保证连续完整的采集会话。已有原始证据继续保留，状态不得伪装为正常完成。
_避免使用_：失败日志、无效会话

**采集分段**：采集会话中一次连续附加到确定目标进程的区间。承载进程消失后，对新进程的附加属于同一采集会话的新分段，中断区间必须保留。
_避免使用_：子会话、新会话

**原始观察**：采集边界在确定时间、连接和采集分段中实际看到的完整字节。相同数据经过多个边界时，每次原始观察都保留；它们之间的去重或对应关系属于后续分析。
_避免使用_：重复数据、去重帧

**逻辑数据关联**：基于时间、连接、方向、长度和内容等证据，将多个原始观察标记为可能来自同一逻辑数据。关联不能删除或替代任何原始观察。
_避免使用_：去重

**采集工作区**：用户首次运行时选择并由 MiniTrace 后续复用的本地目录，是全部采集会话及其索引的写入边界。
_避免使用_：缓存目录、输出目录

**平台采集能力**：桌面采集工具在特定操作系统和微信版本上，经真实双向流量验证的明文采集能力。界面可运行不等于平台采集能力已经成立。
_避免使用_：跨平台支持

**平台接入**：某个平台的采集后端已经遵循 MiniTrace 的启动、事件、停止和会话接口，但尚未经过该平台当前微信版本的真实流量验收。平台接入不等于平台采集能力。
_避免使用_：支持、兼容

**支持环境**：平台、CPU 架构、微信版本和采集配置均已匹配，并通过《世界 Online》登录、资源请求、双向心跳、移动、切图和战斗真实流量验收的组合。当前 macOS 支持范围只包含 Apple Silicon。
_避免使用_：可运行环境、理论兼容环境

**兼容性诊断**：在未知微信版本或不匹配环境中，只检查进程、模块和候选采集边界，不产生正式采集会话，也不宣称正在采集。
_避免使用_：兼容模式、尽力抓包

**候选进程**：经进程角色、已加载模块和微信版本检查后，可能承载《世界 Online》流量的微信子进程。候选进程只有在协议流量验证后才成为当前采集目标。
_避免使用_：微信 PID、首个匹配进程

**实时视图**：采集会话在界面中的有界投影，用于确认流量、筛选和检查近期游戏协议帧。实时视图可以丢弃旧的显示项，但采集会话不能因此丢失原始证据。
_避免使用_：实时会话、完整记录

**磁盘水位警告**：剩余空间低于安全水位时持续显示的采集风险。警告不自动停止采集或删除历史采集会话，是否停止由用户决定。
_避免使用_：磁盘保护、自动清理
